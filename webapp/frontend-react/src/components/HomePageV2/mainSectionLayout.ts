// MainSection のレイアウト定数。
// コンポーネントと同じファイルに置くと react-refresh/only-export-components の警告が出るため、
// 定数だけ別ファイルに切り出している。

export const MAIN_SECTION_MAX_WIDTH = 640;

export const MAIN_SECTION_HEIGHT = {
  xs: 520,
} as const;
