# モーダル角丸をカード方針に合わせる設計ノート

## タスク概要
- 目的: 「やった！でもおなかいっぱいでもう今日は動けないのだ」などのメッセージモーダルの角丸を、カードと同じ丸み方針へ揃える
- 背景: カード角丸対応後、モーダルだけデフォルト角丸のままで統一感が不足

## 今回の要望（原文要約）
- モーダルの角丸が未対応だったので、前回同様にプランナー/レビュワー分業で修正したい

## 詳細設計（プランナー記入欄）
- 対象モーダル
  - `webapp/frontend-react/src/pages/HomePage.tsx` のメッセージ表示 `Dialog`
  - 対象メッセージ例: 「やった！でもおなかいっぱいでもう今日は動けないのだ」など、`responseText` を表示するダイアログ
- 変更方針
  - カード方針（`022`）と見た目をそろえるため、モーダル本体（`Dialog` の `Paper`）の角丸を通常カードと同じ `14px` に合わせる
  - 変更対象は角丸のみ。余白・文字サイズ・ボタン配置・アニメーションは変更しない
  - 角丸指定は `Dialog` 内部の `Paper` に限定し、ほかのコンポーネントへ影響を広げない
- 想定実装箇所
  - `HomePage.tsx` の `Dialog open={openDialog}` 定義箇所
  - MUI v7 の指定方法に合わせ、`Dialog` の `slotProps.paper.sx.borderRadius`（または同等の `Paper` 向け指定）で `14px` を設定
- 確認方法
  - ゲームを進めてメッセージ `Dialog` を表示し、モーダル角丸がカード（`14px`）と同系統に見えるか目視確認
  - PC 幅とスマホ幅で、角の見え方に違和感がないか確認
  - `OK` ボタン押下、モーダルの開閉、背景操作（`onClose`）が変更前と同じ挙動か確認
  - 差分レビュー時に、角丸以外のスタイル変更が混ざっていないことを確認

## レビュー（レビュワー記入欄）
- ここにレビュワーが指摘または LGTM 判定を記入する

## 実装メモ（プランナー記入欄）
- ※ここは実装前メモ（まだ実装しない）
- 実装予定手順
  1. `HomePage.tsx` の `Dialog` 定義を開き、`Paper` のスタイル指定方法（`slotProps` など）を確認する
  2. `Dialog` の `Paper` に `borderRadius: "14px"` を追加し、カード方針と統一する
  3. ローカル表示でメッセージモーダルを出し、PC/スマホ相当幅で見た目と操作を確認する
  4. 角丸以外の差分が入っていないかセルフレビューする
  5. 実装後にこのノートへ「実績値・確認結果」を追記できるよう、確認観点をチェックリスト化する
