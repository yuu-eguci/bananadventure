エンディング到達時のアチーブメント表示
===

## タスク概要
- `scene 14` または `scene 15` 到達時に、アチーブメント表示 UI を出す。
- 表示対象は `トゥルーエンド` `バッドエンド` `全アイテムコンプ` の 3 つ。
- 取得済み / 未取得を同時表示して見分けられる状態にする。

## 今回の要望 (統合者まとめ)
- エンディング到達時に、味気ない単文ではなく UI としてアチーブメントを表示したい。
- 取得できたものとできなかったものを、同じ画面内で判別可能にしたい。

## 変更対象
- `docs/agent/017-エンディング到達時のアチーブメント表示.md`
- `webapp/frontend-react/src/pages/HomePage.tsx`

## 詳細設計
### 表示条件
- アチーブメントカードは `scene.id === 14` または `scene.id === 15` のときに表示する。
- それ以外のシーンでは表示しない。

### 判定条件
- `トゥルーエンド`: 現在シーンが `scene 14` のとき取得済み。
- `バッドエンド`: 現在シーンが `scene 15` のとき取得済み。
- `全アイテムコンプ`: 現在プレイヤー所持アイテム ID が、`scenes.json` 内で取得可能な全アイテム ID をすべて含むとき取得済み。
- `全アイテムコンプ` 判定は、配列の長さ比較ではなく ID の `Set` 同士の包含判定で行う。

### UI デザイン方針
- 右カラムへ `エンディングアチーブメント` カードを追加する。
- カード内に 3 行 (3 アチーブメント) を常時表示する。
- 各行は以下の要素で構成する。
  - 左: アチーブメント名 + 補足説明 (判定条件)
  - 右: `取得済み` または `未取得` のステータスバッジ
- 取得済み行は緑系の背景/枠、未取得行はグレー系の背景/枠で視覚的に区別する。
- モバイル幅でも崩れないように、行コンテナは `justifyContent: space-between` + `gap` を使い、バッジは `whiteSpace: nowrap` を指定する。

### 実装方針
- `HomePage.tsx` で `sceneData` を読み、取得可能アイテム ID の集合をモジュールスコープで作る。
- `viewModel` から現在シーン ID とプレイヤー所持アイテムを使って、3 アチーブメントの配列を生成する。
- 生成した配列を map で描画し、3 項目すべてを常時表示する。
- `viewModel` が `null` の初期化中はアチーブメント計算を行わず、既存表示のみとする。

### 期待効果
- エンディング時の達成状況が一画面で明確に把握できる。
- 「取れたもの / 取れなかったもの」の判別が即時にできる。

## レビュー

### レビュワー指摘 (1 回目)
1. 所持アイテムは重複追加される可能性があるため、`全アイテムコンプ` 判定は ID の集合比較で行うことを明記してください。
2. `viewModel` 未初期化時に計算しないよう、ガード条件を設計へ追記してください。
3. モバイル幅でカードが崩れないよう、ステータスバッジの折り返しと行高さ方針を追記してください。

判定: `Needs Fix`

### プランナー対応 (1 回目)
1. `全アイテムコンプ` は ID の `Set` 比較で判定する方針を追記しました。
2. `viewModel` 未初期化時のガード方針を追記しました。
3. モバイル幅のレイアウト方針 (gap / nowrap) を追記しました。

### レビュワー再レビュー (2 回目)
- 指摘 1 から 3 の反映を確認しました。
- 判定ロジックと UI 条件が実装に落とし込み可能な粒度です。

判定: `LGTM`

## 実装結果
- `webapp/frontend-react/src/pages/HomePage.tsx` に、エンディング専用の `エンディングアチーブメント` カードを追加しました。
- `scene 14/15` 到達時のみカードを表示し、以下 3 項目を常時表示する実装にしました。
  - `トゥルーエンド`
  - `バッドエンド`
  - `全アイテムコンプ`
- 各項目は `取得済み / 未取得` バッジで明確に区別し、未取得項目も非表示にせず常時表示します。
- `全アイテムコンプ` 判定は `scenes.json` から抽出した全取得可能アイテム ID と、プレイヤー所持アイテム ID の `Set` 比較で判定します。

## 検証結果
- `npx tsc -b` で型チェック成功。
- 終端シーン以外でカードが表示されないこと、終端シーンで 3 項目すべて表示されることをコード上で確認。

## オーナー向け要約
- 要望どおり、エンディング到達時にアチーブメント UI を表示するようにしました。
- 「取得できたもの」と「取得できなかったもの」を同時に見分けられるデザインです。
- ただのメッセージではなく、ステータスカード形式で視認性を上げています。
